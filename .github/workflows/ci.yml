name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  ZIG_VERSION: 0.16.0
  # Node 22+ is required: tests are .ts and run via Node's native type stripping.
  NODE_VERSION: 22

jobs:
  lint:
    name: Lint (zig fmt, tsc, prettier)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run lint

  test:
    name: Test (${{ matrix.os }})
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci

      # A Windows DLL must resolve napi_*/uv_* at link time, so grab Node's import
      # library for the runner's exact version and point the build at it.
      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          ver="$(node -p process.versions.node)"
          curl -sSLo node.lib "https://nodejs.org/dist/v${ver}/win-x64/node.lib"
          zig build -Doptimize=ReleaseFast -Dnode-lib="$PWD/node.lib"
          npm run build:ts

      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: npm run build

      - run: npm test
