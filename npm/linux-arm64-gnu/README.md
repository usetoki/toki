# `toki-linux-arm64-gnu`

Prebuilt **aarch64-unknown-linux-gnu** binary for [`toki`](https://www.npmjs.com/package/@usetoki/toki).

You don't install this directly — it's an optional dependency of `toki`, pulled in automatically on matching platforms.
