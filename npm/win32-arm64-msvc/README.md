# `@usetoki/toki-win32-arm64-msvc`

Prebuilt **aarch64-pc-windows-msvc** binary for [`@usetoki/toki`](https://www.npmjs.com/package/@usetoki/toki).

Installed automatically as an optional dependency of `@usetoki/toki` on matching platforms — don't install it directly.
