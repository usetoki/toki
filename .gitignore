# Zig
/zig-out
/.zig-cache

# Node
/node_modules
/dist

# Native addon binary (rebuilt by `npm run build`, shipped via npm `files`)
*.node

# Logs / env / OS
*.log
npm-debug.log*
.env
.env.*
.DS_Store

# Claude Code session artifacts
.claude/
